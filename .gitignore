# -----------------------------------------------------------------------------
# .gitignore for a Full-Stack Project (e.g., Next.js Frontend + Node.js Backend)
# Place this file in the root directory of your repository.
# -----------------------------------------------------------------------------

# =============================================================================
# GLOBAL / OS Specific
# =============================================================================
.DS_Store
.env* # IMPORTANT: Do NOT commit environment variables!
.vscode/ # VS Code settings, if you don't want to share them
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.pnpm-store/

# =============================================================================
# FRONTEND (Next.js specific)
# Located in your 'frontend' directory (e.g., frontend/)
# =============================================================================
frontend/out/
frontend/.next/ # Next.js build output
frontend/node_modules/
frontend/.env # Local environment variables (often symlinked from .env.local)
frontend/.env.local # Local environment variables for Next.js
frontend/.env.development.local
frontend/.env.production.local
frontend/npm-debug.log*
frontend/yarn-debug.log*
frontend/yarn-error.log*
frontend/.next/cache # Next.js cache directory
frontend/public/uploads/ # If you have a local uploads folder
frontend/.next/types/ # Generated TypeScript types from Next.js (can be large)

# Sanity Studio specific (if it's nested within frontend or directly in root)
# Adjust path if your Sanity studio is not in `frontend/studio`
# If your studio is in a separate root folder like `studio/` then update path
frontend/studio/.next/
frontend/studio/node_modules/
frontend/studio/.env
frontend/studio/.env.local
frontend/studio/.sanity/
frontend/studio/.schema
frontend/studio/.vscode/ # Dev environment specific sanity settings
frontend/studio/dist/ # Sanity studio build output

# =============================================================================
# BACKEND (Node.js/Express/MERN specific)
# Located in your 'server' directory (e.g., server/)
# =============================================================================
server/node_modules/
server/.env # Local environment variables for your backend
server/npm-debug.log*
server/yarn-debug.log*
server/yarn-error.log*
server/build/ # If your backend has a build step (e.g., TypeScript compilation)
server/dist/ # Another common output folder for compiled backend code
server/logs/ # Any server-side log files
server/tmp/ # Temporary files generated by the server

# =============================================================================
# GENERAL PROJECT FILES (if any at the root)
# =============================================================================
# .idea/ # JetBrains IDEs (IntelliJ, WebStorm, etc.)
# *.sublime-project
# *.sublime-workspace
# .history/ # VS Code local history
# .DS_Store